---
  provisionerId: "aws-provisioner"
  workerType: "b2gtest"
  payload:
    image: "ubuntu:latest"
    command:
      - "/bin/bash"
      - "-c"
      - "git clone ${GITHUB_HEAD_REPO_URL} && cd redo && git checkout ${GITHUB_HEAD_SHA} && ./runtests.sh"
    maxRunTime: 3600
  metadata:
    name: "Redo - Utilities to retry Python callables"
    description: "Redo - Utilities to retry Python callables"
    owner: "bhearsum@mozilla.com"
    source: "https://github.com/bhearsum/redo"
